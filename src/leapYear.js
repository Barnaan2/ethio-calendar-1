//====Method to test whether the year is leap or not===
  export function isLeapYear(year){
   let modules = year % 4
   return modules === 0 ? true:false  
  }
  // console.log(isLeapYear(2012))
  // console.log(isLeapYear(2014))

